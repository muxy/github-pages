# Communication API

The MEDKit API offers administrative services for back-end communication among extensions.

The [Broadcast Messaging](https://docs.muxy.io/reference/broadcast) service allows extensions to communicate.

The `broadcast` service enables a broadcaster or admin to define and publish messages to subscribers.
A client can broadcast messages to all or some viewers, and also publish and subscribe to message threads.